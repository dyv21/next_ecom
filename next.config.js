/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/next_ecom",
  assetPrefix: "/next_ecom",
  trailingSlash: true,
  output: "export",
  reactStrictMode: true,
};

module.exports = nextConfig;
